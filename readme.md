## Installation
1. run the following command in the project directory: `py -m venv .VirtualEnvironment`
2. then install packages `py -m pip install -r requirements.txt`
3. run the project `py main.py`
